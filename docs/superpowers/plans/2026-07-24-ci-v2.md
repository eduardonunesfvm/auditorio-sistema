# CI v2 - De/Para + Editor Rico + Tipos de Documento

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar campos De/Para, editor rico TinyMCE, tipos de documento (CI normal / Solicitação de Material) e rodapés condicionais com assinatura + recebimento à Comunicação Interna.

**Architecture:** Novas colunas no modelo `ComunicacaoInterna` (+ `tipo`, `de`, `para`, `nome_signatario`, `sobrenome_signatario`, `cargo_signatario`). Template Jinja2 refeito com blocos condicionais por tipo. Frontend ganha select de tipo, campos De/Para/signatário, e TinyMCE via CDN no lugar do textarea.

**Tech Stack:** Python 3, FastAPI, SQLAlchemy, Alembic, Jinja2, WeasyPrint, TinyMCE 6 (CDN), vanilla JS/HTML/CSS

## Global Constraints

- Numeração `numero_ci` única e compartilhada entre tipos (1-100 cíclico)
- TinyMCE carregado via CDN: `https://cdn.tiny.cloud/1/no-api-key/tinymce/6/tinymce.min.js`
- Dados da Érika hardcoded no service (nome, cargo, decreto)
- Sem decreto na CI normal; sem recebimento na CI normal
- Campos signatário (nome/sobrenome/cargo) obrigatórios apenas quando tipo = comunicacao_interna
- Busca textual filtra por de, para, titulo, signatário

---

### Task 1: Migration + Model

**Files:**
- Create: `projeto-auditorio/alembic/versions/<auto>_add_ci_v2_columns.py`
- Modify: `projeto-auditorio/app/models.py:55-70`

**Interfaces:**
- Produces: `ComunicacaoInterna.tipo`, `.de`, `.para`, `.nome_signatario`, `.sobrenome_signatario`, `.cargo_signatario`

- [ ] **Step 1: Generate migration**

```bash
Set-Location "projeto-auditorio"; alembic revision --autogenerate -m "add_ci_v2_columns"; Set-Location ..
```

- [ ] **Step 2: Apply migration**

```bash
Set-Location "projeto-auditorio"; alembic upgrade head; Set-Location ..
```

Expected: columns added to `comunicacoes_internas` table.

- [ ] **Step 3: Update model**

Modify `projeto-auditorio/app/models.py:55-70` — replace the `ComunicacaoInterna` class:

```python
class ComunicacaoInterna(Base):
    __tablename__ = "comunicacoes_internas"

    id: Mapped[uuid.UUID] = mapped_column(Uuid, primary_key=True, default=uuid.uuid4)
    numero_ci: Mapped[int] = mapped_column(Integer, nullable=False)
    tipo: Mapped[str] = mapped_column(String(50), nullable=False, default="comunicacao_interna")
    de: Mapped[str] = mapped_column(String(255), nullable=False, default="")
    para: Mapped[str] = mapped_column(String(255), nullable=False, default="")
    titulo: Mapped[str] = mapped_column(String(255), nullable=False)
    descricao: Mapped[str] = mapped_column(Text, nullable=False)
    data: Mapped[date] = mapped_column(Date, nullable=False)
    nome_signatario: Mapped[Optional[str]] = mapped_column(String(255), nullable=True)
    sobrenome_signatario: Mapped[Optional[str]] = mapped_column(String(255), nullable=True)
    cargo_signatario: Mapped[Optional[str]] = mapped_column(String(255), nullable=True)
    usuario_id: Mapped[uuid.UUID] = mapped_column(ForeignKey("usuarios.id", ondelete="CASCADE"), nullable=False)
    created_at: Mapped[datetime] = mapped_column(DateTime, nullable=False, default=datetime.utcnow)

    usuario: Mapped["Usuario"] = relationship("Usuario", back_populates="comunicacoes_internas")

    @property
    def criador_nome(self) -> str:
        return self.usuario.nome if self.usuario else ""
```

- [ ] **Step 4: Verify model loads**

```bash
Set-Location "projeto-auditorio"; python -c "from app.models import ComunicacaoInterna; print('OK')"; Set-Location ..
```

Expected: `OK`

- [ ] **Step 5: Commit**

```bash
git add projeto-auditorio/app/models.py projeto-auditorio/alembic/versions/
git commit -m "feat: add ci v2 columns - tipo, de, para, signatario fields"
```

---

### Task 2: Schemas

**Files:**
- Modify: `projeto-auditorio/app/schemas.py:72-94`

**Interfaces:**
- Produces: `ComunicacaoInternaCreate` with `tipo`, `de`, `para`, `nome_signatario`, `sobrenome_signatario`, `cargo_signatario`
- Produces: `ComunicacaoInternaResponse` with those fields
- Produces: `ComunicacaoInternaUpdate` with those fields as Optional

- [ ] **Step 1: Replace schemas**

Modify `projeto-auditorio/app/schemas.py:72-94`:

```python
class ComunicacaoInternaCreate(BaseModel):
    tipo: str = Field(..., max_length=50, min_length=1)
    de: str = Field(..., max_length=255, min_length=1)
    para: str = Field(..., max_length=255, min_length=1)
    titulo: str = Field(..., max_length=255, min_length=1)
    descricao: str = Field(..., min_length=1)
    data: date
    nome_signatario: Optional[str] = Field(None, max_length=255)
    sobrenome_signatario: Optional[str] = Field(None, max_length=255)
    cargo_signatario: Optional[str] = Field(None, max_length=255)


class ComunicacaoInternaResponse(BaseModel):
    id: UUID
    numero_ci: int
    tipo: str
    de: str
    para: str
    titulo: str
    descricao: str
    data: date
    nome_signatario: Optional[str] = None
    sobrenome_signatario: Optional[str] = None
    cargo_signatario: Optional[str] = None
    usuario_id: UUID
    criador_nome: str
    created_at: datetime

    model_config = ConfigDict(from_attributes=True)


class ComunicacaoInternaUpdate(BaseModel):
    tipo: Optional[str] = None
    de: Optional[str] = None
    para: Optional[str] = None
    titulo: Optional[str] = None
    descricao: Optional[str] = None
    data: Optional[date] = None
    nome_signatario: Optional[str] = None
    sobrenome_signatario: Optional[str] = None
    cargo_signatario: Optional[str] = None
```

- [ ] **Step 2: Commit**

```bash
git add projeto-auditorio/app/schemas.py
git commit -m "feat: update ci schemas with v2 fields"
```

---

### Task 3: PDF Template

**Files:**
- Modify: `projeto-auditorio/app/templates/ci_template.html`

**Interfaces:**
- Consumes: `banner_src`, `numero_ci`, `tipo`, `titulo`, `de`, `para`, `descricao` (HTML), `data`, `nome_signatario`, `sobrenome_signatario`, `cargo_signatario`, `is_solicitacao` (bool)
- Produces: Styled A4 PDF with conditional footer blocks

- [ ] **Step 1: Rewrite template**

Write `projeto-auditorio/app/templates/ci_template.html`:

```html
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <style>
    @page {
      size: A4;
      margin: 2cm;
    }
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: Arial, Helvetica, sans-serif;
      font-size: 12pt;
      color: #000;
      line-height: 1.6;
    }
    .banner {
      width: 100%;
      max-width: 500px;
      margin: 0 auto 30px;
      display: block;
    }
    .numero-ci {
      font-size: 18pt;
      font-weight: bold;
      text-align: center;
      margin: 30px 0;
      padding: 10px;
      border-top: 2px solid #000;
      border-bottom: 2px solid #000;
    }
    .info-row {
      margin-bottom: 12px;
    }
    .info-label {
      font-weight: bold;
      display: inline-block;
      min-width: 60px;
    }
    .info-value {
      display: inline;
    }
    .descricao-box {
      margin: 25px 0;
      padding: 15px;
      border: 1px solid #ccc;
    }
    .descricao-box .label {
      font-weight: bold;
      margin-bottom: 8px;
    }
    .descricao-box table {
      border-collapse: collapse;
      width: 100%;
      margin: 10px 0;
    }
    .descricao-box table td, .descricao-box table th {
      border: 1px solid #999;
      padding: 6px 8px;
    }
    .descricao-box table th {
      background-color: #f0f0f0;
      font-weight: bold;
    }
    .fechamento {
      margin-top: 40px;
      text-align: justify;
    }
    .assinatura {
      margin-top: 60px;
      text-align: center;
    }
    .linha-assinatura {
      border-top: 1px solid #000;
      width: 350px;
      margin: 0 auto 8px;
      padding-top: 5px;
    }
    .assinatura-nome {
      font-weight: bold;
      font-size: 12pt;
    }
    .assinatura-cargo {
      font-size: 10pt;
      color: #444;
      margin-bottom: 4px;
    }
    .assinatura-decreto {
      font-size: 9pt;
      color: #555;
    }
    .carimbo {
      margin: 15px auto 0;
      width: 200px;
      height: 60px;
      border: 1px dashed #999;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 9pt;
      color: #999;
    }
    .recebimento {
      margin-top: 50px;
      font-size: 10pt;
    }
    .recebimento .linha-recebimento {
      border-top: 1px solid #000;
      width: 350px;
      margin-top: 20px;
      padding-top: 4px;
    }
    .rodape {
      position: fixed;
      bottom: -1.5cm;
      left: 0;
      right: 0;
      text-align: center;
      font-size: 8pt;
      color: #999;
    }
  </style>
</head>
<body>
  {% if banner_src %}<img src="{{ banner_src }}" class="banner" alt="Banner">{% endif %}

  <div class="numero-ci">COMUNICA&Ccedil;&Atilde;O INTERNA{% if is_solicitacao %} - SOLICITA&Ccedil;&Atilde;O DE MATERIAL{% endif %} N&ordm; {{ numero_ci }}</div>

  <div class="info-row">
    <span class="info-label">Data:</span>
    <span class="info-value">{{ data }}</span>
  </div>

  <div class="info-row">
    <span class="info-label">De:</span>
    <span class="info-value">{{ de }}</span>
  </div>

  <div class="info-row">
    <span class="info-label">Para:</span>
    <span class="info-value">{{ para }}</span>
  </div>

  <div class="info-row">
    <span class="info-label">T&iacute;tulo:</span>
    <span class="info-value">{{ titulo }}</span>
  </div>

  <div class="descricao-box">
    <div class="label">Descri&ccedil;&atilde;o:</div>
    {{ descricao | safe }}
  </div>

  <div class="fechamento">
    Sendo o que tenho para o momento, desde j&aacute; agrade&ccedil;o a vossa valorosa e prestimosa aten&ccedil;&atilde;o.
  </div>

  {% if is_solicitacao %}
  <div class="assinatura">
    <div class="linha-assinatura"></div>
    <div class="assinatura-nome">&Eacute;rika Siqueira Souza Battistelli</div>
    <div class="assinatura-cargo">Superintendente de Gest&atilde;o, Educa&ccedil;&atilde;o, Humaniza&ccedil;&atilde;o em sa&uacute;de</div>
    <div class="assinatura-decreto">Decreto n&ordm; 340, de 16/07/2025</div>
    <div class="carimbo">Espa&ccedil;o para carimbo</div>
  </div>

  <div class="recebimento">
    <div>Recebimento em: _____/_____/______</div>
    <div class="linha-recebimento">Assinatura e carimbo (ou nome completo): _______________________________</div>
  </div>
  {% else %}
  <div class="assinatura">
    <div class="linha-assinatura"></div>
    <div class="assinatura-nome">{{ nome_signatario }} {{ sobrenome_signatario }}</div>
    <div class="assinatura-cargo">{{ cargo_signatario }}</div>
  </div>
  {% endif %}
</body>
</html>
```

- [ ] **Step 2: Commit**

```bash
git add projeto-auditorio/app/templates/ci_template.html
git commit -m "feat: rewrite ci template with de/para, conditional footer, table support"
```

---

### Task 4: Service

**Files:**
- Modify: `projeto-auditorio/app/service.py:129-225`

**Interfaces:**
- Modifies: `_render_template` — new params: `tipo`, `de`, `para`, `nome_signatario`, `sobrenome_signatario`, `cargo_signatario`
- Modifies: `criar_e_gerar_pdf`, `gerar_pdf_por_id`, `atualizar_ci` — pass new fields

- [ ] **Step 1: Replace `_render_template` and all CI methods**

Replace lines 129-225 in `projeto-auditorio/app/service.py`:

```python
class ComunicacaoInternaService:
    ERIKA = {
        "nome": "Erika Siqueira Souza Battistelli",
        "cargo": "Superintendente de Gestao, Educacao, Humanizacao em saude",
        "decreto": "Decreto nº 340, de 16/07/2025",
    }

    def __init__(self, repo: ComunicacaoInternaRepository):
        self.repo = repo
        template_dir = os.path.join(os.path.dirname(__file__), "templates")
        self.jinja_env = Environment(loader=FileSystemLoader(template_dir))
        banner_path = os.path.join(template_dir, "secretariabanner.png")
        self.banner_src = self._load_banner_base64(banner_path)

    def _load_banner_base64(self, path: str) -> str:
        try:
            with open(path, "rb") as f:
                data = base64.b64encode(f.read()).decode("utf-8")
            return f"data:image/png;base64,{data}"
        except Exception:
            return ""

    def _render_template(self, numero_ci: int, tipo: str, de: str, para: str, titulo: str, descricao: str, data_str: str, nome_signatario: str, sobrenome_signatario: str, cargo_signatario: str) -> str:
        template = self.jinja_env.get_template("ci_template.html")
        is_solicitacao = tipo == "solicitacao_material"
        return template.render(
            banner_src=self.banner_src,
            numero_ci=numero_ci,
            is_solicitacao=is_solicitacao,
            de=de,
            para=para,
            titulo=titulo,
            descricao=descricao,
            data=data_str,
            nome_signatario=nome_signatario or "",
            sobrenome_signatario=sobrenome_signatario or "",
            cargo_signatario=cargo_signatario or "",
        )

    def _calcular_proximo_numero(self) -> int:
        max_numero = self.repo.obter_max_numero_ci()
        proximo = (max_numero or 0) + 1
        return ((proximo - 1) % 100) + 1

    def criar_e_gerar_pdf(self, dados: ComunicacaoInternaCreate, usuario: Usuario) -> bytes:
        numero_ci = self._calcular_proximo_numero()

        nova_ci = ComunicacaoInterna(
            numero_ci=numero_ci,
            tipo=dados.tipo,
            de=dados.de,
            para=dados.para,
            titulo=dados.titulo,
            descricao=dados.descricao,
            data=dados.data,
            nome_signatario=dados.nome_signatario,
            sobrenome_signatario=dados.sobrenome_signatario,
            cargo_signatario=dados.cargo_signatario,
            usuario_id=usuario.id,
        )
        self.repo.criar(nova_ci)

        html_renderizado = self._render_template(
            numero_ci=numero_ci,
            tipo=nova_ci.tipo,
            de=nova_ci.de,
            para=nova_ci.para,
            titulo=nova_ci.titulo,
            descricao=nova_ci.descricao,
            data_str=nova_ci.data.strftime("%d/%m/%Y"),
            nome_signatario=nova_ci.nome_signatario or "",
            sobrenome_signatario=nova_ci.sobrenome_signatario or "",
            cargo_signatario=nova_ci.cargo_signatario or "",
        )

        pdf_bytes = HTML(string=html_renderizado).write_pdf()
        return pdf_bytes

    def gerar_pdf_por_id(self, ci_id: UUID) -> bytes:
        ci = self.repo.buscar_por_id(ci_id)
        if not ci:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Comunicacao Interna nao encontrada.")

        html_renderizado = self._render_template(
            numero_ci=ci.numero_ci,
            tipo=ci.tipo,
            de=ci.de,
            para=ci.para,
            titulo=ci.titulo,
            descricao=ci.descricao,
            data_str=ci.data.strftime("%d/%m/%Y"),
            nome_signatario=ci.nome_signatario or "",
            sobrenome_signatario=ci.sobrenome_signatario or "",
            cargo_signatario=ci.cargo_signatario or "",
        )

        pdf_bytes = HTML(string=html_renderizado).write_pdf()
        return pdf_bytes

    def atualizar_ci(self, ci_id: UUID, dados: ComunicacaoInternaUpdate, usuario: Usuario) -> bytes:
        ci = self.repo.buscar_por_id(ci_id)
        if not ci:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Comunicacao Interna nao encontrada.")

        self.repo.atualizar(ci, dados)

        html_renderizado = self._render_template(
            numero_ci=ci.numero_ci,
            tipo=ci.tipo,
            de=ci.de,
            para=ci.para,
            titulo=ci.titulo,
            descricao=ci.descricao,
            data_str=ci.data.strftime("%d/%m/%Y"),
            nome_signatario=ci.nome_signatario or "",
            sobrenome_signatario=ci.sobrenome_signatario or "",
            cargo_signatario=ci.cargo_signatario or "",
        )

        pdf_bytes = HTML(string=html_renderizado).write_pdf()
        return pdf_bytes

    def listar_cis(self, usuario: Usuario) -> list[ComunicacaoInterna]:
        if usuario.role == UserRole.ADMIN:
            return self.repo.listar_todas()
        return self.repo.listar_por_usuario(usuario.id)
```

- [ ] **Step 2: Verify service imports**

```bash
Set-Location "projeto-auditorio"; python -c "from app.service import ComunicacaoInternaService; print('OK')"; Set-Location ..
```

Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add projeto-auditorio/app/service.py
git commit -m "feat: update ci service with v2 fields, erika constant, conditional rendering"
```

---

### Task 5: Frontend HTML

**Files:**
- Modify: `auditorio-front/index.html:144-217` (CI tab section)

**Interfaces:**
- Adds: select `ci-tipo`, inputs `ci-de`, `ci-para`, `ci-nome`, `ci-sobrenome`, `ci-cargo`, TinyMCE script
- Produces: Form fields that JS can read/write

- [ ] **Step 1: Replace CI form section in HTML**

Replace lines 155-181 of `auditorio-front/index.html` (the `<form id="ci-form">` block):

```html
                <form id="ci-form" novalidate>
                  <input type="hidden" id="ci-editing-id" value="">
                  <div class="form-group">
                    <label for="ci-tipo">Tipo de Documento</label>
                    <select id="ci-tipo" name="ci_tipo" required>
                      <option value="comunicacao_interna">Comunicacao Interna</option>
                      <option value="solicitacao_material">Solicitacao de Material</option>
                    </select>
                  </div>
                  <div class="form-group">
                    <label for="ci-de">De (Setor de Origem)</label>
                    <input type="text" id="ci-de" name="ci_de" placeholder="Ex: Secretaria da Saude" required maxlength="255">
                  </div>
                  <div class="form-group">
                    <label for="ci-para">Para (Setor de Destino)</label>
                    <input type="text" id="ci-para" name="ci_para" placeholder="Ex: Gabinete do Prefeito" required maxlength="255">
                  </div>
                  <div class="form-group">
                    <label for="ci-titulo">Titulo</label>
                    <input type="text" id="ci-titulo" name="ci_titulo" placeholder="Ex: Convocacao para Reuniao" required>
                  </div>
                  <div class="form-group">
                    <label for="ci-data">Data</label>
                    <input type="date" id="ci-data" name="ci_data" required>
                  </div>
                  <div id="ci-signatario-fields">
                    <div class="form-row">
                      <div class="form-group">
                        <label for="ci-nome">Nome</label>
                        <input type="text" id="ci-nome" name="ci_nome" placeholder="Seu nome" required maxlength="255">
                      </div>
                      <div class="form-group">
                        <label for="ci-sobrenome">Sobrenome</label>
                        <input type="text" id="ci-sobrenome" name="ci_sobrenome" placeholder="Seu sobrenome" required maxlength="255">
                      </div>
                    </div>
                    <div class="form-group">
                      <label for="ci-cargo">Cargo</label>
                      <input type="text" id="ci-cargo" name="ci_cargo" placeholder="Seu cargo" required maxlength="255">
                    </div>
                  </div>
                  <div class="form-group">
                    <label for="ci-descricao">Descricao</label>
                    <textarea id="ci-descricao" name="ci_descricao" rows="4" placeholder="Descreva o conteudo da comunicacao..."></textarea>
                  </div>
                  <div id="ci-feedback" class="feedback" aria-live="polite"></div>
                  <div class="form-actions">
                    <button type="submit" class="btn btn-primary btn-full" id="ci-btn">Gerar e Baixar CI</button>
                    <button type="button" class="btn btn-outline btn-full" id="ci-cancel-edit-btn" style="display:none;">Cancelar</button>
                  </div>
                </form>
```

- [ ] **Step 2: Update table header**

Replace lines 195-203 of `auditorio-front/index.html` (the `<thead>` of `tabela-cis`):

```html
                      <thead>
                        <tr>
                          <th>Nº</th>
                          <th>Tipo</th>
                          <th>Titulo</th>
                          <th>De</th>
                          <th>Para</th>
                          <th>Data</th>
                          <th>Emitido por</th>
                          <th class="th-actions">Acoes</th>
                        </tr>
                      </thead>
```

Replace the `colspan` in the empty row (line 206):

```html
                        <tr class="empty-row">
                          <td colspan="8" class="empty-state">Nenhuma Comunicacao Interna encontrada.</td>
                        </tr>
```

- [ ] **Step 3: Add TinyMCE CDN script**

Add before `</body>` (line 227), before `<script src="app.js"></script>`:

```html
  <script src="https://cdn.tiny.cloud/1/no-api-key/tinymce/6/tinymce.min.js"></script>
```

- [ ] **Step 4: Commit**

```bash
git add auditorio-front/index.html
git commit -m "feat: update ci form with tipo, de/para, signatario fields, tinymce cdn"
```

---

### Task 6: Frontend JS

**Files:**
- Modify: `auditorio-front/app.js:570-816` (all CI-related functions)

**Interfaces:**
- Consumes: DOM elements `ci-tipo`, `ci-de`, `ci-para`, `ci-nome`, `ci-sobrenome`, `ci-cargo`, TinyMCE instance
- Produces: Updated `renderTabelaCIs`, `loadCIs`, form submit handler, edit handler, search handler

- [ ] **Step 1: Add TinyMCE init and tipo toggle functions**

Add after `var cisCache = [];` (line 570):

```javascript
var ciTinyInited = false;

function initTinyMCE() {
  if (ciTinyInited) return;
  var ta = document.getElementById("ci-descricao");
  if (!ta || typeof tinymce === "undefined") return;
  tinymce.init({
    target: ta,
    plugins: "table lists link",
    toolbar: "bold italic underline | bullist numlist | table | undo redo",
    menubar: false,
    statusbar: false,
    height: 250,
    setup: function (ed) {
      ed.on("change", function () {
        ed.save();
      });
    },
  });
  ciTinyInited = true;
}

function toggleSignatarioFields() {
  var tipo = document.getElementById("ci-tipo").value;
  var fields = document.getElementById("ci-signatario-fields");
  var isCI = tipo === "comunicacao_interna";
  if (fields) fields.style.display = isCI ? "" : "none";
  var nome = document.getElementById("ci-nome");
  var sobrenome = document.getElementById("ci-sobrenome");
  var cargo = document.getElementById("ci-cargo");
  if (nome) nome.required = isCI;
  if (sobrenome) sobrenome.required = isCI;
  if (cargo) cargo.required = isCI;
}

function getCITinyContent() {
  if (typeof tinymce !== "undefined" && tinymce.get("ci-descricao")) {
    return tinymce.get("ci-descricao").getContent();
  }
  return document.getElementById("ci-descricao").value;
}

function setCITinyContent(html) {
  if (typeof tinymce !== "undefined" && tinymce.get("ci-descricao")) {
    tinymce.get("ci-descricao").setContent(html || "");
  } else {
    document.getElementById("ci-descricao").value = html || "";
  }
}

function resetCIForm() {
  document.getElementById("ci-editing-id").value = "";
  document.getElementById("ci-form").reset();
  document.getElementById("ci-tipo").value = "comunicacao_interna";
  document.getElementById("ci-btn").textContent = "Gerar e Baixar CI";
  document.getElementById("ci-cancel-edit-btn").style.display = "none";
  setCITinyContent("");
  toggleSignatarioFields();
  var feedback = document.getElementById("ci-feedback");
  feedback.textContent = "";
  feedback.className = "feedback";
}
```

- [ ] **Step 2: Update `switchTab` to init TinyMCE**

Replace line 585-588 in `switchTab`:

```javascript
  if (tabName === "ci") {
    atualizarEstadoCI();
    loadCIs();
    setTimeout(initTinyMCE, 100);
  }
```

- [ ] **Step 3: Update `renderTabelaCIs`**

Replace lines 619-642:

```javascript
function renderTabelaCIs(cis) {
  var tbody = document.getElementById("tabela-cis-body");
  if (!cis || cis.length === 0) {
    tbody.innerHTML = '<tr class="empty-row"><td colspan="8" class="empty-state">Nenhuma Comunicacao Interna encontrada.</td></tr>';
    return;
  }

  function emitente(ci) {
    if (ci.tipo === "solicitacao_material") return "Erika Siqueira Souza Battistelli";
    if (ci.nome_signatario) return ci.nome_signatario + (ci.sobrenome_signatario ? " " + ci.sobrenome_signatario : "");
    return ci.criador_nome || "";
  }

  function tipoLabel(tipo) {
    if (tipo === "solicitacao_material") return "Sol. Material";
    return "CI";
  }

  function stripHtml(html) {
    if (!html) return "";
    var div = document.createElement("div");
    div.innerHTML = html;
    return div.textContent || div.innerText || "";
  }

  var html = "";
  for (var i = 0; i < cis.length; i++) {
    var ci = cis[i];
    html +=
      '<tr>' +
        '<td>' + ci.numero_ci + '</td>' +
        '<td>' + escapeHtml(tipoLabel(ci.tipo)) + '</td>' +
        '<td>' + escapeHtml(ci.titulo) + '</td>' +
        '<td>' + escapeHtml(ci.de || "-") + '</td>' +
        '<td>' + escapeHtml(ci.para || "-") + '</td>' +
        '<td>' + formatDate(ci.data) + '</td>' +
        '<td>' + escapeHtml(emitente(ci)) + '</td>' +
        '<td class="td-actions">' +
          '<button class="btn btn-sm btn-edit editar-ci-btn" data-id="' + ci.id + '" title="Editar">Editar</button>' +
          '<button class="btn btn-sm btn-edit baixar-ci-btn" data-id="' + ci.id + '" title="Baixar PDF">PDF</button>' +
        '</td>' +
      '</tr>';
  }
  tbody.innerHTML = html;
}
```

- [ ] **Step 4: Update search/filter**

Replace lines 807-816:

```javascript
var buscaCIsInput = document.getElementById("busca-cis");
if (buscaCIsInput) {
  buscaCIsInput.addEventListener("input", function () {
    var query = buscaCIsInput.value.toLowerCase().trim();
    var filtrados = query ? cisCache.filter(function (ci) {
      var emitenteNome = ci.nome_signatario || ci.criador_nome || "";
      var sobrenome = ci.sobrenome_signatario || "";
      return ci.titulo.toLowerCase().indexOf(query) !== -1
        || (ci.de || "").toLowerCase().indexOf(query) !== -1
        || (ci.para || "").toLowerCase().indexOf(query) !== -1
        || emitenteNome.toLowerCase().indexOf(query) !== -1
        || sobrenome.toLowerCase().indexOf(query) !== -1;
    }) : cisCache;
    renderTabelaCIs(filtrados);
  });
}
```

- [ ] **Step 5: Update search placeholder**

Replace line 191 of `auditorio-front/index.html`:

```html
                    <input type="text" id="busca-cis" placeholder="Buscar por titulo, de, para ou emitente..." autocomplete="off">
```

- [ ] **Step 6: Update CI form submit handler**

Replace lines 669-734:

```javascript
document.getElementById("ci-form").addEventListener("submit", async function (e) {
  e.preventDefault();
  var feedback = document.getElementById("ci-feedback");
  hideFeedback(feedback);

  if (!temPermissaoCI()) {
    showAlertModal("Voce nao tem permissao para gerar Comunicacao Interna.");
    return;
  }

  var tipo = document.getElementById("ci-tipo").value;
  var de = document.getElementById("ci-de").value.trim();
  var para = document.getElementById("ci-para").value.trim();
  var titulo = document.getElementById("ci-titulo").value.trim();
  var data = document.getElementById("ci-data").value;
  var descricao = getCITinyContent();
  var nomeSignatario = document.getElementById("ci-nome").value.trim();
  var sobrenomeSignatario = document.getElementById("ci-sobrenome").value.trim();
  var cargoSignatario = document.getElementById("ci-cargo").value.trim();
  var editId = document.getElementById("ci-editing-id").value;

  if (!tipo || !de || !para || !titulo || !data || !descricao) {
    showFeedback(feedback, "Preencha todos os campos obrigatorios.", "error");
    return;
  }

  if (tipo === "comunicacao_interna") {
    if (!nomeSignatario || !sobrenomeSignatario || !cargoSignatario) {
      showFeedback(feedback, "Preencha nome, sobrenome e cargo do signatario.", "error");
      return;
    }
  }

  var btn = document.getElementById("ci-btn");
  setButtonLoading(btn, true);

  var isEditing = !!editId;
  var method = isEditing ? "PUT" : "POST";
  var url = isEditing
    ? API_URL + "/api/v1/ci/" + encodeURIComponent(editId)
    : API_URL + "/api/v1/ci";

  var body = {
    tipo: tipo,
    de: de,
    para: para,
    titulo: titulo,
    data: data,
    descricao: descricao,
  };

  if (tipo === "comunicacao_interna") {
    body.nome_signatario = nomeSignatario;
    body.sobrenome_signatario = sobrenomeSignatario;
    body.cargo_signatario = cargoSignatario;
  } else {
    body.nome_signatario = null;
    body.sobrenome_signatario = null;
    body.cargo_signatario = null;
  }

  try {
    var res = await fetch(url, {
      method: method,
      headers: apiHeaders(),
      body: JSON.stringify(body),
    });

    if (!res.ok) {
      handleAuthError(res.status);
      var errData = await res.json().catch(function () { return null; });
      throw new Error(errData?.detail || "Erro ao processar CI.");
    }

    var blob = await res.blob();
    var downloadUrl = URL.createObjectURL(blob);
    var a = document.createElement("a");
    a.href = downloadUrl;
    a.download = "CI_" + titulo.substring(0, 30) + ".pdf";
    document.body.appendChild(a);
    a.click();
    document.body.removeChild(a);
    URL.revokeObjectURL(downloadUrl);

    showFeedback(feedback, isEditing ? "CI atualizada com sucesso!" : "CI gerada com sucesso!", "success");
    resetCIForm();
    loadCIs();
  } catch (err) {
    showFeedback(feedback, err.message, "error");
  } finally {
    setButtonLoading(btn, false);
  }
});
```

- [ ] **Step 7: Update edit handler**

Replace lines 770-795:

```javascript
function preencherFormularioEdicaoCI(ciId) {
  var ci = null;
  for (var i = 0; i < cisCache.length; i++) {
    if (cisCache[i].id === ciId) {
      ci = cisCache[i];
      break;
    }
  }

  if (!ci) {
    showAlertModal("Comunicacao Interna nao encontrada.");
    return;
  }

  document.getElementById("ci-editing-id").value = ci.id;
  document.getElementById("ci-tipo").value = ci.tipo || "comunicacao_interna";
  document.getElementById("ci-de").value = ci.de || "";
  document.getElementById("ci-para").value = ci.para || "";
  document.getElementById("ci-titulo").value = ci.titulo || "";
  document.getElementById("ci-data").value = ci.data || "";
  document.getElementById("ci-nome").value = ci.nome_signatario || "";
  document.getElementById("ci-sobrenome").value = ci.sobrenome_signatario || "";
  document.getElementById("ci-cargo").value = ci.cargo_signatario || "";
  toggleSignatarioFields();

  setTimeout(function () {
    setCITinyContent(ci.descricao || "");
  }, 200);

  document.getElementById("ci-btn").textContent = "Salvar Alteracoes";
  document.getElementById("ci-cancel-edit-btn").style.display = "inline-flex";
  document.getElementById("ci-feedback").textContent = "";
  document.getElementById("ci-feedback").className = "feedback";
  document.getElementById("ci-titulo").focus();
  document.getElementById("ci-form").scrollIntoView({ behavior: "smooth", block: "start" });
}
```

- [ ] **Step 8: Update cancel edit handler**

Replace lines 797-805:

```javascript
document.getElementById("ci-cancel-edit-btn").addEventListener("click", function () {
  resetCIForm();
});
```

- [ ] **Step 9: Add tipo change listener**

Add after the cancel edit handler (after line 805):

```javascript
var ciTipoSelect = document.getElementById("ci-tipo");
if (ciTipoSelect) {
  ciTipoSelect.addEventListener("change", function () {
    toggleSignatarioFields();
  });
}
```

- [ ] **Step 10: Commit**

```bash
git add auditorio-front/app.js auditorio-front/index.html
git commit -m "feat: update ci frontend js - tinymce, tipo toggle, de/para, signatario, search"
```

---

### Task 7: Frontend CSS

**Files:**
- Modify: `auditorio-front/styles.css` (add `form-row-2` rule if needed)

- [ ] **Step 1: Add TinyMCE border fix**

Add after existing `.form-row` rules in `auditorio-front/styles.css`:

```css
.tox-tinymce {
  border-radius: var(--radius) !important;
  border: 1px solid var(--border) !important;
}
```

If `.form-row-2` or `.form-row-3` don't exist yet, find where `.form-row-3` is defined in `styles.css` and add the `.form-row-2` rule adjacent to it.

- [ ] **Step 2: Commit**

```bash
git add auditorio-front/styles.css
git commit -m "style: add form-row-2 grid and tinymce border fix"
```

---

### Task 8: Integration Verification

- [ ] **Step 1: Start backend and verify API**

```bash
Set-Location "projeto-auditorio"; python -c "from app.main import app; print('FastAPI app loads OK')"; Set-Location ..
```

- [ ] **Step 2: Verify template renders**

```bash
Set-Location "projeto-auditorio"; python -c "
from app.service import ComunicacaoInternaService
from app.repository import ComunicacaoInternaRepository
from app.database import SessionLocal
s = ComunicacaoInternaService(None)
html = s._render_template(1, 'comunicacao_interna', 'Saude', 'Gabinete', 'Teste', '<p>OK</p>', '24/07/2026', 'Joao', 'Silva', 'Coordenador')
assert 'Joao Silva' in html
assert 'De:' in html
assert 'Para:' in html
assert 'Recebimento' not in html
print('CI Normal template OK')

html2 = s._render_template(2, 'solicitacao_material', 'Saude', 'Gabinete', 'Teste', '<p>OK</p>', '24/07/2026', '', '', '')
assert 'Erika' in html2
assert 'Recebimento' in html2
assert 'carimbo' in html2
print('Solicitacao Material template OK')
"; Set-Location ..
```

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "chore: final integration verification"
```
